---
const references = [
  { name: "Ericka Galeano", phone: "5477-3505" },
  { name: "Jenifer Martinez", phone: "3020-8292" },
  { name: "Dania García", phone: "4572-7641" }
];
---

<section id="referencias" class="py-16 bg-slate-50">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-bold text-slate-900 mb-12 text-center">Referencias Personales</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {references.map((ref) => (
        <div class="bg-white p-6 rounded-lg text-center shadow-sm hover:shadow-md transition-shadow">
          <div class="w-12 h-12 bg-primary/10 text-primary rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
            </svg>
          </div>
          <h3 class="text-lg font-bold text-slate-900">{ref.name}</h3>
          <p class="text-primary font-medium mt-1">{ref.phone}</p>
        </div>
      ))}
    </div>
  </div>
</section>
