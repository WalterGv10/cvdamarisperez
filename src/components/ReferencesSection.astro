---
const references = [
  { name: "Ericka Galeano", phone: "5477-3505" },
  { name: "Jenifer Martinez", phone: "3020-8292" },
  { name: "Dania García", phone: "4572-7641" }
];
---

<section id="referencias" class="py-24 bg-white relative overflow-hidden">
  <div class="absolute bottom-0 left-0 w-80 h-80 bg-vibrant/5 rounded-full blur-3xl"></div>
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <h2 class="text-4xl font-bold text-slate-900 mb-16 text-center reveal text-backdrop">
      Referencias <span class="text-purple">Personales</span>
    </h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {references.map((ref, index) => (
        <div class={`bg-white p-8 rounded-2xl text-center shadow-sm border border-slate-100 hover:shadow-2xl hover:shadow-purple/10 hover:-translate-y-2 transition-all duration-300 reveal delay-${index * 100}`}>
          <div class="w-16 h-16 bg-purple/10 text-purple rounded-full flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform">
            <svg class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
            </svg>
          </div>
          <h3 class="text-xl font-bold text-slate-900">{ref.name}</h3>
          <p class="text-vibrant font-bold mt-2 text-lg">{ref.phone}</p>
        </div>
      ))}
    </div>
  </div>
</section>
